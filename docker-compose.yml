version: "3.8"
services:
  app:
    build:
      context: ./spyglass-backend
    ports:
      - "8080:8080"
    # The application will now use the datasource configuration from its internal application.yml
    # Make sure your application.yml is correctly configured to connect to the external database.

