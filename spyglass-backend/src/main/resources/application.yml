spring:
  application:
    name: spyglass-backend
  datasource:
    url: jdbc:mysql://${DB_HOST:shcamz.xyz}:${DB_PORT:3306}/${DB_NAME:amzspaglass}?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: ${DB_USER:amzspaglass}
    password: ${DB_PASSWORD:RYna5tjK7nKwZd7J}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: false

server:
  port: ${PORT:8081}

# Logging configuration
logging:
  config: classpath:logback-spring.xml

  management:
    endpoints:
      web:
        exposure:
          include: health,info
    endpoint:
      health:
        show-details: never

# Spring Doc OpenAPI 配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

# 爬虫配置
scraper:
  fixedDelayMs: ${SCRAPER_FIXED_DELAY_MS:14400000} # 调度器轮询间隔（毫秒），默认4小时（生产）
  proxy:
    enabled: true
    providers:
      - name: novproxy
        type: RESIDENTIAL
        url: us.novproxy.io:1000
        username: sehx5222-region-US
        password: ygazcakb
  htmlDumpEnabled: true
  htmlDumpDir: logs/html-dump
  randomDelayMinMs: 150
  randomDelayMaxMs: 450
  maxRetry: 3
