spring:
  datasource:
    url: jdbc:mysql://shcamz.xyz:3306/amzspaglass?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: amzspaglass
    password: RYna5tjK7nKwZd7J
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

server:
  port: ${PORT:8080}

# Logging configuration
logging:
  file:
    name: spyglass.log # 指定日志文件名
  level:
    root: INFO
    com.amz.spyglass: DEBUG # 将应用包的日志级别设为 DEBUG
    org.hibernate.SQL: DEBUG # 打印 Hibernate SQL
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE # 打印 SQL 参数绑定

# Spring Doc OpenAPI 配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

# 爬虫配置
scraper:
  fixedDelayMs: 3000 # 调度器轮询间隔（毫秒），3秒用于快速测试
  proxy:
    enabled: true
    providers:
      - name: novproxy
        type: RESIDENTIAL
        url: us.novproxy.io:1000
        username: sehx5222-region-US
        password: ygazcakb
  htmlDumpEnabled: true
  htmlDumpDir: logs/html-dump
  randomDelayMinMs: 150
  randomDelayMaxMs: 450
  maxRetry: 3
